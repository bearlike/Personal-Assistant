Aider List Directory tool

Use this tool to list files under a local directory.

Tool inputs (JSON):
- path: directory or file path to list (defaults to ".").
- root: optional project root to resolve file paths (defaults to current working directory).
- max_entries: optional integer limit for number of entries returned.

Provide the arguments required to list entries.
